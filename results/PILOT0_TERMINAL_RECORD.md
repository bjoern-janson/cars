# Pilot 0 — Terminal Record

## Status

```text
PILOT 0
STATUS: CLOSED
```

Pilot 0 is closed because the evidence has reduced the live explanatory space to a point where no further experiment is presently justified by a sufficiently discriminating unresolved question.

This is an **epistemic stopping rule**, not a claim of certainty, theoretical completeness, or zero residual uncertainty.

```text
STOP
≠ truth
≠ completeness
≠ certainty

STOP
= no presently justified escalation
```

The terminal correction cycle is:

```text
failure detected
→ hypothesis localized
→ alternatives discriminated
→ surviving effects replicated
→ effect sizes bounded
→ endpoint-specific uncertainty preserved
→ further complexity fails to earn authorization
→ STOP
```

## Scope

The terminal record applies only to the frozen Pilot 0 artificial-system program:

```text
model: Qwen/Qwen3-4B
revision: 1cfa9a7208912126459214e8b04321603b3df60c
benchmark: MMLU-Pro
answer interface: first-generated-token-valid-option-letter
thinking: disabled
```

Nothing here automatically transports to other models, benchmarks, interfaces, interventions, or domains.

## Original moderation hypothesis

The original Pilot 0 hypothesis was:

```text
higher I₁
→ greater causal benefit from verified-error feedback
```

where:

```text
I₁ = 1 - P(correct)
```

Terminal status:

```text
H1
NOT SUPPORTED
```

`I₁` showed modest criterion validity for initial error likelihood, but correction-related validity was not demonstrated.

```text
error detection
≠ correction susceptibility
```

## Representation-localization lineage

The representation branch progressed only when the previous result earned a narrower discriminating question.

```text
A3
full interface-form bundle
→ T_change / T_instability / T_verified causal

A4a
conversational topology
→ T_verified causal

A4b
historical-prestate rendering
→ T_change causal

A4c
legacy vs canonical inline rendering
→ T_change / T_instability / T_verified causal

A5
prior-state prose vs fields
→ T_change causal
→ T_verified causal
→ T_instability practically small

A6
section labels present vs absent
→ T_change causal
→ T_instability practically small
→ T_verified practically small

A7
encoding × scaffold factorial
→ T_change causal interaction
→ T_instability practically small interaction
→ T_verified interaction unresolved
```

The unresolved `T_verified` interaction remains endpoint-local:

```text
unresolved T_verified interaction
↛ reopen T_instability
```

## R1 replication / transport branch

R1 was a new branch with a different scientific purpose:

```text
A-series:
WHERE in the representation bundle is the effect?

R1:
DOES the inherited encoding → T_instability contrast
transport across fresh independently sampled prestates?
```

R1 used four disjoint fresh prestate cohorts from the same sampling distribution, with the exact inherited labeled-scaffold, E0-only prose-versus-fields manipulation.

Cohort effects:

```text
C1  +0.030043
C2  +0.045267
C3  +0.025974
C4  +0.034615
```

Equal-cohort common effect:

```text
Δ_common = +0.033975
95% CI   = [+0.021316,+0.047791]
two-sided blocked-randomization p = 0.00019996
```

Therefore both statements are earned:

```text
encoding → T_instability
NONZERO / REPLICATED

and

encoding → T_instability
PRACTICALLY SMALL at the inherited ±0.05 scale
```

The entire common-effect interval lies strictly inside `[-0.05,+0.05]`.

Transport diagnostic:

```text
Cochran Q = 1.120334
df = 3
p = 0.772168
I² = 0
tau²_DL = 0
```

Authorized interpretation:

```text
observed cohort effects
→ compatible with a common-effect model
```

Not authorized:

```text
compatible with common effect
→ proven transport invariance
```

Excess between-cohort variation was **not detected**. No heterogeneity branch was earned.

## Terminal authority ledger

| Object | Terminal status |
| --- | --- |
| `T_change` × prior-state encoding | **CAUSAL** |
| `T_change` × section scaffold | **CAUSAL** |
| `T_change` × encoding×scaffold | **CAUSAL INTERACTION** |
| `T_verified` × prior-state encoding | **CAUSAL** |
| `T_verified` × conversational topology | **CAUSAL** |
| `T_verified` × section scaffold | **PRACTICALLY SMALL** |
| `T_verified` × encoding×scaffold | **UNRESOLVED** |
| `T_instability` × broad inline representation | **CAUSAL** |
| `T_instability` × prior-state encoding | **CAUSAL / REPLICATED / PRACTICALLY SMALL** |
| `T_instability` × section scaffold | **PRACTICALLY SMALL** |
| `T_instability` × encoding×scaffold | **PRACTICALLY SMALL** |
| `T_instability` × excess cohort variation | **NOT DETECTED** |
| transport invariance | **NOT ESTABLISHED** |
| global correction capacity | **NOT ESTABLISHED** |
| psychological mechanism | **NOT ESTABLISHED** |
| general intelligence claim | **NOT ESTABLISHED** |

Additional frozen statuses remain:

```text
B      UNRESOLVED
C      UNRESOLVED
CARS   FROZEN
```

## Stopping rule

The Pilot 0 branch is not closed because nothing else could theoretically be tested.

It is closed because nothing else is presently justified to test.

```text
sufficiently contracted live hypothesis space
+
alternatives discriminated
+
surviving effects replicated / bounded
+
remaining uncertainty locally contained
+
no sufficiently discriminating next experiment
────────────────────────────────────────────
                        ↓
                      STOP
```

Therefore:

```text
A-series representation localization
→ sufficiently resolved

R1 encoding→instability replication/transport
→ closed

A8
→ NOT EARNED

R2
→ NOT EARNED

additional T_instability decomposition
→ NOT EARNED
```

Any future experiment requires:

```text
new scientific question
+
new pre-outcome justification
```

Residual curiosity, an unresolved endpoint elsewhere, or the availability of additional formatting ablations does not itself authorize reopening Pilot 0.

## Terminal methodological compression

```text
CORRECTION
→ contraction of the live hypothesis space
→ not perpetual hypothesis generation
```

Pilot 0 demonstrates that successful correction can terminate: once the live explanatory space has contracted sufficiently and no unresolved question supports a superior discriminating experiment, further revision is not epistemically required.

## Authority ceiling

Pilot 0 does **not** establish:

- a global correction-capacity trait;
- a general intelligence measure or law;
- a psychological mechanism for the representation effects;
- cross-model or cross-domain transport;
- transport invariance;
- that the unresolved `T_verified` interaction must be pursued;
- that CARS itself is empirically validated by the assay.

Scoped causal transition effects remain scoped causal transition effects.
